# CLAUDE.md

> **This file is generated by `./.unicli-rules/sync.sh`. Do not edit directly.**
> Edit the sources under `.unicli-rules/` (`project-context.md`, `core-workflow.md`, `memory/*.md`).

---

## Project Context

{{PROJECT_CONTEXT}}

---

## Core Workflow

{{CORE_WORKFLOW}}

---

## Memory (canonical seed)

{{MEMORY}}

---

## AI CLI Integration

This project uses the **unicli-hub** template pattern:

- Canonical source: `.unicli-rules/`
- Derived directories: `.claude/`, `.cursor/`, `.gemini/`, `.kiro/`, `.codex/`
- Regeneration: `./.unicli-rules/sync.sh --fix`
- Protection: the PreToolUse hook configured in `.claude/settings.local.json` blocks direct edits to generated files.

## Available Agents

Sub-agents in `.claude/agents/`:

- `researcher` — investigation
- `codegen` — implementation
- `reviewer` — review

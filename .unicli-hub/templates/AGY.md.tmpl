# AGY.md

> **Location:** repository root (`AGY.md`). Antigravity CLI (agy) agents/skills live under `.agents/`.

> **This file is generated by `./sync.sh`. Do not edit directly.**
> Edit the sources under `hub/` (`project-context.md`, `core-workflow.md`, `memory/*.md`).

---

## Project Context

{{PROJECT_CONTEXT}}

---

## Core Workflow

{{CORE_WORKFLOW}}

---

## Memory

{{MEMORY}}
---
{{PLUGINS}}

---

## Using the Antigravity CLI (agy)

- Agents: `.agents/agents/<name>/agent.json`
- Skills: `.agents/skills/<name>/SKILL.md`
- Settings: `.agents/mcp_config.json`
- Regenerate: `./sync.sh --fix`

---

## Shared Hub Concept

This project uses **UniCLI-Hub** to share context across multiple AI tools. 
Whether you use `agy`, `claude`, `gemini`, or `cursor`, you are looking at the same **Single Source of Truth** defined in `hub/`.

# CLAUDE.md

> **This file is generated by `./sync.sh`. Do not edit directly.**
> Edit the sources under `hub/` (`project-context.md`, `core-workflow.md`, `memory/*.md`).

---

## Project Context

{{PROJECT_CONTEXT}}

---

## Core Workflow

{{CORE_WORKFLOW}}

---

## Memory (canonical seed)

{{MEMORY}}

---

## AI CLI Integration

This project uses the **unicli-hub** template pattern:

- Canonical source: `hub/`
- Derived directories: `.antigravitycli/`, `.claude/`, `.cursor/`, `.kiro/`, `.codex/`, `.grok/`
- Regeneration: `./sync.sh --fix`
- Protection: the PreToolUse hook configured in `.claude/settings.local.json` blocks direct edits to generated files.

## Available Agents

Sub-agents in `.claude/agents/`:

- `researcher` — investigation
- `codegen` — implementation
- `reviewer` — review

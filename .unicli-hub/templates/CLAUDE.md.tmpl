# CLAUDE.md

> **This file is generated by `./sync.sh`. Do not edit directly.**
> Edit the canonical sources under `hub/`.

---

## Project Context

{{PROJECT_CONTEXT}}

---

## Generated File Rules

**Never edit derived files directly**:

- `CLAUDE.md`, `AGENTS.md` at the repository root
- `.agents/agents/*/agent.json`, `.agents/skills/*/SKILL.md`, `.agents/settings.json`
- `.claude/agents/*.md`, `.claude/skills/*/SKILL.md`, `.claude/settings.json`
- `.cursor/rules/*.mdc`, `.cursor/agents/*.md`, `.cursor/skills/*/SKILL.md`, `.cursor/hooks.json`
- `.kiro/steering/*.md`
- `.codex/agents/*.toml`
- `.grok/agents/*.md`, `.grok/skills/*/SKILL.md`, `.grok/hooks/*.json`, `.grok/rules/*.md`, `.grok/config.toml`
- MCP: `.mcp.json`, `.cursor/mcp.json`, `.kiro/settings/mcp.json`, `.agents/mcp_config.json`, `.codex/config.toml`, `.grok/config.toml`

**Edit here instead**: `hub/`, `.unicli-hub/templates/`, and `hub/registry/`.
After editing, run `./sync.sh --fix` to regenerate every derived file.

---

## Memory

{{MEMORY}}

---

## AI CLI Integration

This repository is a **unicli-hub** template/framework workspace:

- Registry source: `hub/registry/`
- Agent source: `hub/agents/`
- Skill source: `hub/skills/`
- Hook source: `hub/hooks/`
- MCP source: `hub/mcp-servers.json`
- Regeneration: `./sync.sh --fix`
- Drift check: `./sync.sh --check`
- Protection: generated-file hooks block direct edits to derived files.
